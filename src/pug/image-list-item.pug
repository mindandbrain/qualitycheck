.image-list-item
  header
    .sub(ref="subLabel")
    .task(ref="taskLabel")
    .run(ref="runLabel")
    .sources.expandable(ref="sourcesLabel")
  img(ref="image")
  footer
    button.green
    button.yellow
    button.red
    button.clear