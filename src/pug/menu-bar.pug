nav.menu-bar
  .title
    | mindandbrain/qualitycheck
    span(ref="versionNumberContainer")
  .spacer
  .button-group.ratings
    header Ratings
    button(ref="openButton") Open
    button(ref="saveButton") Save
    button.warn(ref="resetButton") Reset
  .button-group.view
    header View
    .segmented-control.sort-by(ref="sortByContainer")
      header Sort by
    .segmented-control.zoom(ref="toggleZoomContainer")
      header Zoom
    .segmented-control.orientation(ref="orientationContainer")
      header Orientation
  .button-group.search
    input(type="search" 
          placeholder="Go to" 
          spellcheck="false" 
          autocomplete="false"
          ref="searchInput") 
    ul.autocomplete-suggestions(ref="searchAutocompleteSuggestions")